var squared = function (x) {
  var squaredNumber = x * x;
  return squaredNumber;
};

var plus10 = function(x) {
  var newNumber = x+ 10;
  return newNumber;
};

var numbers = {
  "squared": squared,
  "plus10": plus10
};


//for the html (as reference in class)
    //var y = numbers.squared(22);
    //console.log(y);

    //var x = numbers.plus10(2);
    //console.log(x);
